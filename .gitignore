# Secrets and credentials (never commit)
.env
token.txt

# Large API cache (regenerated locally; warm cache speeds re-runs)
.proxytool_cache/

# Python / notebook artifacts
__pycache__/
*.pyc
.ipynb_checkpoints/

# Editor and local tooling
.cursor/
.git-backups/
.vscode/
.idea/

# System files
.DS_Store
*.log

# Redundant local exports / duplicates
proxytool4.ipynb
proxytool copy.ipynb.txt
proxytool.py.txt
changes.ipynb
TestHarness_NEEDSTOKEN.ps1

# Local-only notes and drafts
INSTRUCTIONS.md
linkedin_post.txt

# MetaMatch live run outputs (snapshots live under runs/experiments/)
#runs/manual-ml-py/
#runs/_summaries/

# Root-level document exports (assets/ papers remain tracked)
/*.docx
/*.pdf

# Off-repo tarballs (grid history archived locally)
# archives/off_repo/

# REDUX: track core + repro scripts (see REDUX_REPRO.md). Exclude only the
# large exploratory notebook copy inside the package; regenerate redux4_core.py
# with scripts/extract_redux4_core.py from proxytool_REDUX_4.ipynb.


# projected_pairs/ and scripts/ are tracked (Spearman/Pearson stats + repro harness).
# results_benchmark/ canonical outputs (labeled/, anchorsv2_redux/, queryv2_redux/,
# projected_pairs/, archives/) are tracked.

# Partial / in-progress independent anchorsv2 rerun (non-canonical workspace)

# InsForge & AI agent skills
.insforge
.agent
.agents
.augment
.claude
.cline
.github/copilot*
.kilocode
.qoder
.qwen
.roo
.trae
.windsurf

reference/insforge/