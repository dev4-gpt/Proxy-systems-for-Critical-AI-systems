# Ignore local secrets and caches
# Token files
.env
token.txt

# Notebook checkpoints and cache
.ipynb_checkpoints/
.proxytool_cache/

# Editor and local-only artifacts
.cursor/
.git-backups/
__pycache__/
*.pyc

# Redundant local exports / duplicates
proxytool4.ipynb
proxytool copy.ipynb.txt
proxytool.py.txt
changes.ipynb
TestHarness_NEEDSTOKEN.ps1

# Results and large generated outputs (local / regenerated)
results_plots/
analysis/

# MetaMatch: live run outputs (regenerated every pipeline run; commit snapshots under runs/experiments/)
runs/manual-ml-py/
runs/_summaries/

# Local-only notes and drafts (never commit — if Git still tracks them, run:
#   git rm -r --cached analysis/ INSTRUCTIONS.md linkedin_post.txt
INSTRUCTIONS.md
linkedin_post.txt

# Logs and system files
*.log
.DS_Store

# Keep repository metadata tracked
!.gitignore

.gitattributes
apply_guard.py
NextCompareReqs.pdf
notebook_deep_analysis.json

# REDUX 4 scoring core, repro runbook, and scripts are tracked so a fresh clone
# can re-score (see REDUX_REPRO.md). Only the large exploratory notebook copy
# inside the package stays out of git; regenerate redux4_core.py with
# scripts/extract_redux4_core.py from proxytool_REDUX_4.ipynb.
proxytool_redux/proxytool.ipynb

# projected_pairs/ outputs are tracked (Spearman/Pearson stats survive a clone).