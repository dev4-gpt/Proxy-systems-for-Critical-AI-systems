# Off-repo archives

Grid history and exploratory sweeps were tarball'd here before removal from the git tree.

- **`metamatch_grid_history.tar.gz`** — MetaMatch penalty grid folders, `redux4_sweep/`, `custom_30_pairs/`, stale experiment docs.

Restore locally: `tar -xzf metamatch_grid_history.tar.gz` from repo root.

This directory is gitignored; keep the tarball locally if you need the historical runs.
